# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Security: Prevent sensitive data commits
# Terraform files with real data
terraform.tfvars
terraform.tfvars.json
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Environment files with real configuration
.env
.env.local

# IDE
.vscode/
.idea/

# Temporary config files (contains personal/project data)
**/current-buckets.json
config-*.json
project-config.json
bucket-config.json
**/terraform.tfvars

# Temporary deployment files (contain secrets during deployment)
service.yaml
# cloudrun-service.yaml - commenting out since template contains only placeholders

# Keep templates (they contain no secrets, only placeholders)
!.github/templates/

# Additional custom ignores
service-account*.json
firebase-adminsdk*.json
google-cloud-key*.json
*.log
cloudrun-service-updated.yaml

# IDE files
.vscode/settings.json
.idea/
*.swp
*.swo

# Build artifacts
dist/
.turbo/
